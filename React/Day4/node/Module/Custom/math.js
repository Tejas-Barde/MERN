const Add = (a, b) => a + b;
const Substract = (a, b) => a - b;
const Multiply = (a, b) => a * b;
const Divide = (a, b) => a / b;

const Math = {
  Add,
  Substract,
  Multiply,
  Divide
};

module.exports = Math;
